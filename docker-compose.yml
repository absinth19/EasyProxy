services:
  easyproxy:
    build: .
    container_name: easyproxy
    restart: unless-stopped
    ports:
      - "7860:7860"
    environment:
      - CONFIG_DIR=/data
      - RECORDINGS_DIR=/data/recordings
      #- API_PASSWORD=
      - PORT=7860
    volumes:
      - ./data:/data
